language: c
sudo: required
install: wget https://raw.githubusercontent.com/ocaml/ocaml-travisci-skeleton/master/.travis-opam.sh
script: bash -ex .travis-opam.sh
env:
  - OCAML_VERSION=3.12 PACKAGE=ocamllint
  - OCAML_VERSION=4.00 PACKAGE=ocamllint
  - OCAML_VERSION=4.02 PACKAGE=ocamllint
